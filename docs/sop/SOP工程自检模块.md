# SOP工程自检模块 v1.2

> 创建：2026-05-04 | 更新 v1.1：2026-05-04 | 更新 v1.2：2026-05-11
> v1.2 变更：新增 CKP-1c（AI味审计报告验证，v6.9），增加AI味审计相关日志记录条目。
> 定位：工具稳定性检查，与写作质量检查（步骤3/7/8）互补
> 范围：产物文件完整性、跨Agent文件传递可靠性、写入操作正确性
> 原则：每个检查点 ≤ 3次工具调用、≤ 5秒完成、不依赖Agent判断、**异常发现必须通知用户**

---

## 设计动机

SOP v6.6 已有写作质量检查全覆盖（步骤3质检 + 步骤7自进化 + 步骤8回顾），但缺少**工程质量**的系统性检查：

| 已发生的事故 | 缺失的检查 |
|-------------|-----------|
| Ch9 QA报告声称存盘但未落盘 | 步骤3后无文件存在性验证 |
| 浓缩包27MB/271K行编码损坏 | 追加操作后无行数/可读性验证（v6.6补了） |
| write_file 覆盖风险 | 写入前无 read_file 确认 |
| Agent输出路径偏离 | 无 glob 验证文件确实在指定位置 |

**一句话**：SOP管"写得好不好"，不管"存没存对、读不读得出"。

---

## ⚠️ 铁律：异常必须通知用户

> **教训（浓缩包损坏事件）**：调度者在 Ch16 步骤5发现浓缩包膨胀至186K+行，判定为"低优先级（辅助产物，不影响下一章生产）"，未通知用户。Ch17-Ch20 每次追加后文件持续恶化至271K行，调度者仍保持沉默。用户直到问题升级才知情。

**这条铁律的核心逻辑**：

```
发现异常 → 通知用户 → 一起判定优先级 → 执行
                    ↑
              NOT:
发现异常 → 调度者单方面判定优先级 → 执行（或沉默）
```

**为什么单方面判定是错的**：

| 我可能不知道的 | 后果 |
|---------------|------|
| 你打算用浓缩包做别的事（检索、导出、分析） | 我判定"不影响生产线"但影响你的其他用途 |
| 小异常是更大问题的前兆 | 186K行→271K行是线性恶化，但我当成了孤立事件 |
| 修复的时机窗口 | 早修5分钟 vs 晚修几小时——你来决定值不值得打断当前流程 |

**执行规则**：

1. **任何 CKP 检查点发现异常（🟡/🔴/🔴🔴）→ 在继续流程之前通知用户**
2. 通知格式：`⚠️ [CKP-X] 异常：xxx（当前值 vs 正常值）。建议：xxx。是否继续/暂停/修复？`
3. 🟡 级别：通知但不阻塞下一步（用户可在阅读消息后选择忽略）
4. 🔴 级别：通知并暂停，等待用户确认后再执行修复
5. 🔴🔴 级别：立即暂停，通知用户，不执行任何自动修复
6. **同类型异常连续发现2次以上 → 升级通知级别**（🟡→🔴）

**浓缩包事件对照**：

| Ch | 实际行为 | 应该的行为 |
|----|---------|-----------|
| Ch16 | 发现186K行→记录memory→沉默 | 通知：⚠️ 浓缩包186K行（预期~160行），建议修复。继续Ch16还是先修？ |
| Ch17 | 追加→恶化→沉默 | 通知：⚠️ 浓缩包持续膨胀（第2次），建议立即重建。 |
| Ch18-19 | 追加→恶化→沉默 | 第3次→🔴 强制暂停 |
| Ch20 | 追加→271K行→写memory | 如果前面通知了，Ch20 之前已经修好 |

---

## 四个检查点

```
步骤0 ──→ CKP-0: 前置文件就绪
步骤1 ──→ CKP-1: 主笔输出验证
步骤2 ──→ CKP-1: 编辑输出验证  （CKP-1 对步骤1/2/3通用）
步骤2.5 → CKP-1c: AI味审计报告验证（v6.9 新增）
步骤3 ──→ CKP-1: 质检输出验证
步骤4 ──→ CKP-2: 正本存盘验证
步骤5 ──→ CKP-3: 浓缩包追加验证
步骤6 ──→ （发用户，不涉及工程检查）
步骤7 ──→ （自进化，不涉及工程检查）
步骤8 ──→ （回顾，不涉及工程检查）
```

---

## CKP-0: 前置文件就绪检查

**时机**：步骤0（准备阶段），在发送步骤1请求之前

**检查项**：

```
□ read_file novel_creator/写作偏好_共享.md (前5行)
  预期：文件存在、首行标题正常、中文无乱码

□ read_file novel_creator/主笔自检清单.md (前5行)
  预期：文件存在、首行标题正常

□ read_file novel_creator/第一卷浓缩包.md (前5行)
  预期：文件存在、章节号连续（末章=当前-1）

□ read_file novel_creator/archives/novel_settings/第一卷详细章纲.md (搜索当前章号)
  预期：文件存在、能定位到当前章纲
```

**判定**：

| 结果 | 动作 |
|------|------|
| 全部通过 | 进入步骤1 |
| 任意文件不存在 | 🔴 暂停，报告缺失文件 |
| 任意文件编码异常 | 🔴 暂停，触发重建 |
| 浓缩包末章号 ≠ 当前-1 | 🟡 检查是否漏更，补齐后继续 |

**⚠️ 浓缩包末章+1 ≠ 当前章号时自动补齐**：说明上章步骤5漏更，从上一章正本读取首段生成摘要，`edit_file` 追加到浓缩包末尾。

---

## CKP-1: Agent输出验证（步骤1/2/3通用）

**时机**：收到Agent回复后，读取其产出前

**步骤1（主笔）额外检查**：

```
□ glob_search "第*章*.md" in agent workspace (如 novel-chapter-writer/chapters/)
  预期：找到1个匹配文件，文件名包含正确章号

□ read_file 该文件 (前5行)
  预期：首行为 # 第X章 标题

□ read_file 该文件 (末5行)
  预期：存在章节完标记或自然段落结尾，中文字符正常
  
□ 字数检查：文件字符数 > 1500 (最少字数阈值)
  预期：通过。若 < 1500 → 🟡 主笔产出不足，发出警告
```

**步骤2（编辑）额外检查**：

```
□ glob_search "第*章*.md" in agent workspace (如 novel-dialogue-editor/)
  预期：找到1个匹配文件

□ read_file 该文件 (前5行 + 末5行)
  预期：章节标题一致、中文无乱码、字数与主笔版偏差 < ±500字
  
□ 字数偏差检查：|编辑版字数 - 主笔版字数| < 500
  预期：通过。若 ≥ 500 → 🟡 编辑大幅增删，检查是否误改情节
```

**步骤3（质检）额外检查**：

```
□ 质检回复中包含评分（如"xx/100"或"通过"）
  预期：能找到评分关键词

□ 若质检声称存盘 → glob 验证文件存在
  预期：文件确实存在（Ch9教训：声称已存但文件不存在）
```

**步骤2.5（AI味审计）额外检查（v6.9 新增）**：

```
□ 审计报告文件是否存在：novel_creator/archives/AI味审计报告/第X章_AI味审计.md
  预期：文件存在、格式完整（含六维度评分+AI味指数+触发指令汇总）

□ 审计报告中AI味指数字段是否可解析
  预期：能找到 "AI味指数：XX/100" 格式的数值

□ 若触发严重（🔴🔴）级别问题 → 检查是否已通知用户
  预期：严重问题必须有用户确认记录才能继续流程
```

**通用：写入操作校验（步骤1/2/3均执行）**：

```
□ 若Agent回复中提及具体文件路径 → 用 read_file 读该路径前3行
  预期：可读、内容与本章标题一致（Ch9教训泛化：任意Agent都可能声称已存但未落盘）
  若文件不存在 → 🔴 CKP-1标记，在继续前要求Agent补存
```

**判定**：

| 结果 | 动作 |
|------|------|
| 全部通过 | 进入下一步骤 |
| 文件不存在（步骤1/2） | 🔴 暂停，重新发送请求（可能是Agent timeout或未完成） |
| 文件编码异常 | 🔴 暂停，重新发送请求 |
| 字数不足（步骤1） | 🟡 警告但继续（质检会判定） |
| 质检未生成报告文件 | 🟡 不阻塞。从质检回复文本中提取评分和意见继续 |

---

## CKP-2: 正本存盘验证

**时机**：步骤4 write_file 之后立即执行

**检查项**：

```
□ glob_search "第X章_标题.md" in novel_creator/chapters/第一卷/
  预期：找到1个匹配文件

□ read_file 该文件 (前3行 + 末3行)
  预期：首行标题正确、末行无截断、中文无乱码

□ 文件大小检查：字节数 3000~15000
  预期：在正常范围。若 < 2000 → 🟡 可能截断；若 > 20000 → 🟡 可能含冗余内容

□ 文件名格式检查：匹配 "第X章_标题.md"
  预期：通过。若不是（如 chapter_0X_final.md）→ 🔴 修正后重新 write_file
```

**判定**：

| 结果 | 动作 |
|------|------|
| 全部通过 | 进入步骤5 |
| 文件不存在 | 🔴 重新 write_file |
| 文件名格式错误 | 🔴 修正文件名后重新 write_file |
| 文件大小异常 | 🟡 读取完整内容人工判断，必要时重新写入 |
| 编码乱码 | 🔴 重新 write_file（指定 encoding='utf-8'） |

---

## CKP-3: 浓缩包追加验证

**时机**：步骤5 edit_file 追加浓缩包之后立即执行

> ⚠️ 此检查点 = v6.6 步骤5健康检查的工程化版本。每章都执行（非仅每5章）。

**轻量版（每章执行）**：

```
□ read_file novel_creator/第一卷浓缩包.md (末30行)
  预期：能找到本章章号、摘要格式完整、中文无乱码

□ 行数检查：总行数 ≈ 预期行数（每章 ~10行）
  预期：总行数 ≤ (章数 × 12) + 5（含格式行和统计行）
  若总行数 > (章数 × 15) → 🔴 触发膨胀告警
```

**完整版（每5章执行，同v6.6健康检查）**：

```
□ read_file 浓缩包末段 ~150行
□ 确认连续3章格式完整
□ 确认无乱码/重复块
```

**判定**：

| 结果 | 动作 |
|------|------|
| 全部通过 | 记录 `[ChX CKP-3 ✅]` |
| 本章摘要未找到 | 🟡 重新执行 edit_file 追加 |
| 行数异常（>预期1.5×） | 🔴 立即重建浓缩包（从各章正本生成） |
| 编码乱码 | 🔴 立即重建浓缩包 |

**行数膨胀自动检测逻辑**：
```
总行数 / (章数 × 10) > 1.5 → 🔴 膨胀告警
总行数 / (章数 × 10) > 3.0 → 🔴🔴 严重膨胀，文件可能已损坏
```

---

## 异常升级规则

| 级别 | 定义 | 动作 |
|------|------|------|
| 🟡 警告 | 异常但不阻塞 | 记录到日志，继续流程 |
| 🔴 暂停 | 阻塞当前步骤 | 自动修复（重写文件/重建浓缩包），修复后继续 |
| 🔴🔴 熔断 | 系统性故障 | 停止流水线，通知用户 |

**自动修复权限**：
- 🔴 级别：可自动修复（重写文件、重建浓缩包、重发Agent请求）
- 🔴🔴 级别：必须等用户确认

---

## 日志记录

每次检查点的结果追加到 `novel_creator/archives/工程自检日志.md`：

```
## ChX | 2026-MM-DD HH:MM

### CKP-0 前置文件
- 写作偏好: ✅ / 浓缩包: ✅ / 章纲: ✅ / 主笔自检清单: ✅
- 异常: 无

### CKP-1 主笔输出
- 文件名: 第X章_标题.md ✅
- 字数: XXXX ✅
- 异常: 无

### CKP-1 编辑输出 (如有)
- 文件名: ✅ | 字数偏差: +XX ✅
- 异常: 无

### CKP-1c AI味审计报告 (v6.9 新增)
- 报告文件: ✅
- AI味指数: XX/100
- 触发指令: X条
- 严重问题: 有/无 → 用户确认: 是/否
- 异常: 无

### CKP-1 质检输出 (如有)
- 评分已提取: ✅
- 异常: 无

### CKP-2 正本存盘
- 路径: novel_creator/chapters/第一卷/第X章_标题.md ✅
- 大小: XXXX字节 ✅
- 异常: 无

### CKP-3 浓缩包
- 末章确认: 第X章 ✅
- 总行数: XXX (预期 ~XXX) ✅
- 异常: 无
```

> 日志按章追加，格式与自进化日志一致。

---

## 与现有SOP的接口

本模块嵌入SOP v6.6各步骤的执行流程中，不作为独立步骤：

```
步骤0: 先跑 CKP-0 → 通过后进入步骤1
步骤1: 主笔回复后 → 跑 CKP-1 → 通过后进入步骤2
步骤2: 编辑回复后 → 跑 CKP-1 → 通过后进入步骤3
步骤3: 质检回复后 → 跑 CKP-1 → 通过后进入步骤4
步骤4: write_file后 → 跑 CKP-2 → 通过后进入步骤5
步骤5: edit_file后 → 跑 CKP-3 → 通过后进入步骤6
```

**不额外增加SOP步骤编号**。这些检查是现有步骤的"安全带"——跟着步骤自动触发，零额外决策。

---

## 成本

每个检查点：2-3次工具调用，~3-5秒。
全章6个检查点总计：~15次工具调用，<30秒。

对比收益：浓缩包损坏一次修复耗时~10分钟。投入产出比 ~20:1。

---

_v1.1 | 2026-05-04 | 从浓缩包损坏事故提取的经验结晶_
