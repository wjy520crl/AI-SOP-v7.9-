# 流水线SOP v7.3 优化方案

> 更新：2026-05-12
> v7.2 → v7.3：**执行强化升级**。针对第一卷挑刺发现的27处问题，优化检测机制+强化执行工具，从源头减少执行漏洞。

---

## 一、问题根因分析

| 问题类型 | 数量 | 根因 | 解决方案 |
|:---|:---:|:---|:---|
| 冻结意象漏检 | 17处 | 执行时未逐句核对清单 | 强制grep扫描脚本 |
| 参数连珠炮 | 2处 | 数字密集未拆解 | 自动统计+预警 |
| 句式重复 | 3处 | D23未覆盖句式模板 | 扩展D23检测范围 |
| 章节引用错误 | 1处 | 人工记忆 | 数字编号强制校验 |
| 跨章状态断裂 | 4处 | 浓缩包更新遗漏 | 自动化diff检查 |

---

## 二、SOP v7.3 核心优化

### 2.1 D23 跨章重复检测扩展（v7.3 优化）

**原检测范围**：意象重复
**扩展后检测范围**：
1. **意象重复**（原有）
2. **句式模板重复**（新增）——检测"嘴角动了一下""手指攥紧"等模板在5章内重复≥3次
3. **章节号引用核对**（新增）——检测"第X章"引用是否与实际章节匹配

**检测算法升级**：
```
句式模板库 = [
  "嘴角*了一下",
  "手指*紧",
  "座椅*硌",
  "第*章"
]

扫描最近5章：
- 模板匹配次数 ≥ 3 → 黄色警告
- 模板匹配次数 ≥ 5 → 红色，强制变化表述
```

---

### 2.2 新增步骤：2.4 强制扫描检查（v7.3 新增）

**插入位置**：步骤2.3（编辑）之后，步骤2.5（AI味审计）之前

**执行清单**：

| 检查项 | 工具 | 阈值 | 不通过动作 |
|:---|:---|:---|:---|
| 冻结意象扫描 | grep | 出现≥1处 | 列出所有匹配行，强制替换 |
| 「不是...是...」计数 | grep | >1处/章 | 列出所有匹配行，强制删减 |
| 参数连珠炮检测 | 正则匹配 | 连续3句含数字 | 标记位置，触发1.8.1 |
| 句式模板重复 | 模板匹配 | 5章内≥3次 | 黄色警告，建议变化 |
| 章节号引用 | 数字提取+比对 | 引用≠实际 | 红色，强制修正 |

**输出格式**：
```
【强制扫描报告】
□ 冻结意象：0处 ✓
□ 「不是...是...」：1处 ✓
□ 参数连珠炮：1处（行89-92）→ 触发1.8.1
□ 句式模板重复：「嘴角动了一下」3章内2次 ✓
□ 章节号引用：0处错误 ✓
```

---

### 2.3 浓缩包字段强制校验（v7.3 新增）

**新增强制字段**：

```yaml
chapter_end_state:
  # 原有字段
  location: ""
  time: ""
  
  # v7.3 新增强制字段
  item_chain_verified: true/false  # 物品链条是否已核对
  cross_ref_checked: true/false    # 跨章引用是否已核对
  frozen_imagery_count: 0          # 本章冻结意象数量
  not_is_but_count: 0              # 「不是...是...」数量
```

**强制校验规则**：
- `item_chain_verified` 必须为 `true` 才能进入下一步
- `frozen_imagery_count` > 0 → 退回编辑
- `not_is_but_count` > 1 → 退回编辑

---

### 2.4 偏差指令1.8.1触发条件放宽（v7.3 优化）

**原触发条件**：连续3句以上每句含数字
**优化后触发条件**：
- 连续3句以上每句含数字 → 黄色警告
- 连续2句含数字且单句>20字 → 黄色警告
- 同一段落内数字密度>30% → 红色，强制触发

**目的**：更早识别参数密集段落，提前拆解

---

## 三、执行强化机制（工具层）

### 3.1 强制扫描脚本（自动化工具）

**脚本名称**：`sop_scanner.py`

**功能**：
```python
def scan_frozen_imagery(text):
    """扫描冻结意象"""
    frozen_list = [
        "指节发白", "嘴角勾起", "倒吸一口凉气",
        "眼窝深陷", "嘴唇干裂", "空气凝固",
        "时间仿佛静止", "折叠椅铁管硌肩胛骨",
        "座椅弹簧硌后背"
    ]
    matches = []
    for item in frozen_list:
        if item in text:
            matches.append(item)
    return matches

def scan_not_is_but(text):
    """扫描「不是...是...」"""
    pattern = r"不是[^，。]+是"
    matches = re.findall(pattern, text)
    return matches

def scan_number_burst(text):
    """扫描参数连珠炮"""
    sentences = split_sentences(text)
    burst_positions = []
    for i in range(len(sentences)-2):
        if has_number(sentences[i]) and has_number(sentences[i+1]) and has_number(sentences[i+2]):
            burst_positions.append(i)
    return burst_positions

def scan_template_repeat(chapter_files, template, window=5):
    """扫描句式模板重复"""
    # 扫描最近window章内的模板出现次数
    pass

def verify_chapter_refs(text, current_chapter):
    """验证章节号引用"""
    refs = extract_chapter_refs(text)  # 提取"第X章"
    errors = []
    for ref in refs:
        if ref != current_chapter and ref not in valid_chapters:
            errors.append(ref)
    return errors
```

**调用方式**：
```bash
python sop_scanner.py --chapter ch15.md --window 5
```

---

### 3.2 预提交检查清单（人工+工具结合）

**每个章节完成后，必须执行**：

```
□ 1. 运行 sop_scanner.py —— 自动化扫描
□ 2. 人工快速浏览 —— 检查扫描器遗漏的语境问题
□ 3. 核对浓缩包 chapter_end_state —— 强制字段填写完整
□ 4. 跨章diff检查 —— 对比上一章状态是否连贯
□ 5. 冻结意象替换确认 —— 所有匹配项已替换
□ 6. 章末钩子验证 —— 存在且非AI收束
□ 7. 签字确认 —— 调度者在报告末尾签字
```

---

### 3.3 熔断机制升级（v7.3）

**原熔断条件**：连续3章质检<90分
**新增熔断条件**：
- 连续2章出现冻结意象 → 暂停，检查扫描脚本
- 同一问题连续3章重复出现 → 暂停，回溯SOP设计
- 浓缩包强制字段缺失 → 禁止进入下一步

---

### 3.4 执行质量追踪（v7.3 新增）

**新增追踪指标**：

| 指标 | 计算方式 | 目标 |
|:---|:---|:---|
| 冻结意象漏检率 | 漏检数/总冻结意象数 | <5% |
| 参数连珠炮拦截率 | 拦截数/总参数连珠炮数 | >90% |
| 跨章引用错误率 | 错误引用数/总引用数 | <2% |
| 强制字段完整率 | 完整字段数/总字段数 | 100% |

**每10章回顾时检查**：
- 若漏检率>5% → 优化扫描脚本
- 若拦截率<90% → 放宽触发条件
- 若引用错误率>2% → 强化章节号校验

---

## 四、v7.2 → v7.3 变更对照表

| 变更项 | v7.2 | v7.3 |
|:---|:---|:---|
| D23检测范围 | 意象重复 | 意象+句式模板+章节号引用 |
| 强制扫描 | 无 | 新增步骤2.4 |
| 浓缩包字段 | 9个 | 9个+3个强制校验字段 |
| 1.8.1触发条件 | 连续3句含数字 | 连续3句/连续2句长句/密度>30% |
| 自动化工具 | 无 | sop_scanner.py |
| 预提交清单 | 简单检查 | 7步强制清单 |
| 熔断条件 | 3章质检<90 | 新增冻结意象/重复问题/字段缺失 |
| 质量追踪 | 无 | 4个指标+10章回顾 |

---

## 五、实施计划

| 阶段 | 任务 | 时间 |
|:---|:---|:---|
| 第1阶段 | 编写sop_scanner.py脚本 | 1天 |
| 第2阶段 | 更新SOP文档（v7.3） | 1天 |
| 第3阶段 | 更新浓缩包模板 | 0.5天 |
| 第4阶段 | 第一卷Ch1-35回溯扫描 | 1天 |
| 第5阶段 | 第二卷试点运行 | 持续 |

---

## 六、预期效果

| 问题类型 | v7.2发生率 | v7.3目标 |
|:---|:---:|:---:|
| 冻结意象 | 17处/35章 | <3处/35章 |
| 参数连珠炮 | 2处/35章 | <1处/35章 |
| 句式重复 | 3处/35章 | <2处/35章 |
| 章节引用错误 | 1处/35章 | 0处 |
| 跨章状态断裂 | 4处/35章 | <1处/35章 |

**核心改进**：从"依赖人工检查"转向"工具强制拦截+人工复核"。
